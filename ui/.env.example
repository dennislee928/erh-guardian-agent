# Base URL of the deployed erh-guardian-mcp worker (NO trailing /mcp — the panel
# only calls the public GET /api/* endpoints). Injected at BUILD time by Vite:
# changing it requires a rebuild, it is not a runtime setting.
VITE_API_URL=https://erh-guardian-mcp.<account>.workers.dev
